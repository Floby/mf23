{{page-title "👸 Miss France Upsized 2024"}}

{{!-- The following component displays Ember's default welcome message. --}}
{{!-- Feel free to remove this! --}}

  <header>
    <h1><LinkTo @route="index">👸 <span title="Miss France Upsized">MFU</span> 2024</LinkTo></h1>
    {{#if this.auth.isAuthenticated}}
      <LinkTo rel="profile" @route="profile" class="profile" title="Profil du juge">
        {{#if this.auth.isExpired}}
          <small class="warning">!</small>
        {{/if}}
        <img src={{if this.profile.autoAvatar (boring-avatar this.profile.nom) this.auth.userInfo.picture}} alt="Aller vers la page de profil" role="link">
      </LinkTo>
    {{/if}}
  </header>
  <main>
<div class={{this.routeStyleNamespaceClassSet}}>
    {{outlet}}
</div>
  </main>
  <footer>
    Made with 💜 by floby
  </footer>
