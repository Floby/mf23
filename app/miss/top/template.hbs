{{page-title "Top"}}
{{outlet}}

<ul class="top">
{{#each @model.top as |rank|}}
<li class="rank">
  <span class="miss" title={{mention-label rank.mention}}>
    {{mention-emoji rank.mention}}
    <LinkTo @route="miss.profile" @model={{rank.miss.id}}> {{rank.miss.région}}</LinkTo>
  </span>
  <span class="mentions" title={{list-join rank.mentions}}>
    <MentionsRow @mentions={{rank.mentions}} />
  </span>
</li>
{{/each}}
</ul>
