{{page-title "Juger"}}
<form class="judge" {{action 'saveJudgement' this.model on="submit"}}>
  <div class="mention-picked">
    {{ this.currentMention.label }}
  </div>
  <fieldset class="mention">
    <label for="mention">Jugement</label>
    <Input @type="range" id="mention" @value={{ this.mention }} name="mention" min="0" max="6" class="heart-slider slider" />
    <ul class="mention-emojis">
      {{#each this.mentions as |mention|}}
        <li>
          <label {{on "click" (fn this.selectMention mention.value)}} for="mention" class="reaction" title={{mention.label}}>{{mention.reaction}}</label>
        </li>
      {{/each}}
    </ul>
  </fieldset>
  <fieldset class="add-comment">
    <Textarea {{input-autosize}} @value={{ this.comment }} placeholder="Ici, la motivation de votre jugement." id="comment" name="comment" cols="40" rows="2" />
    <fieldset class="controls">
      <a class="back" href="javascript:history.back()">&times; annuler</a>
      <input type="submit" value="&plus; enregistrer" />
    </fieldset>
  </fieldset>
</form>
